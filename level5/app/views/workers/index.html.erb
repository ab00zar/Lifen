<div class="row">
  <div class="col-sm-10 col-sm-offset-1">
    <p id="notice"><%= notice %></p>
    <h1>Welcome!</h1>
    <p>Note! The price (salary) of each worker is calculated automatically based on the shifts. The price might be
    adjusted while making, editing and destroying a shift!</p>

    <br>
    <br>

    <h1>Workers</h1>

    <table class="table">
      <thead class="thead-light">
        <tr>
          <th>ID</th>
          <th>First name</th>
          <th>Status</th>
          <th>Salary</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @workers.each do |worker| %>
          <tr>
            <td><%= worker.id %></td>
            <td><%= worker.first_name %></td>
            <td><%= worker.status %></td>
            <td><%= worker.price %></td>
            <td><%= link_to 'Show', worker %></td>
            <td><%= link_to 'Edit', edit_worker_path(worker) %></td>
            <td><%= link_to 'Destroy', worker, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <br>
    <br>
    <table class="table">
      <thead class="thead-light">
      <tr>
        <th>Total price (salary)</th>
        <th>Commission</th>
        <th>No of interim shifts</th>
      </tr>
      </thead>

      <tbody>
        <tr>
          <td><%= Worker.total %></td>
          <td><%= Worker.compute_commission %></td>
          <td><%= Worker.compute_number_of_interim_shifts %></td>
        </tr>
      </tbody>
    </table>
    <br>

    <%= link_to 'New Worker', new_worker_path %>
    <br>

  </div>
</div>